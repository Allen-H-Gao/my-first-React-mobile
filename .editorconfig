# 保证不同IDE下代码文件编码格式的一致性

# 告诉EditorConfig插件，这是根文件，不用继续往上查找
root = true

# 设置字符集
charset = utf-8

# 缩进风格，可选"space"、"tab"
indent_style = tab
# 缩进的空格数
tab_width = 4

# 结尾换行符
end_of_line = lf

# 删除一行中的前后空格
trim_trailing_whitespace = true